# Hardware especifico de esta maquina.
# Generado por: nixos-generate-config
# Ruta real en el sistema: /etc/nixos/hardware-configuration.nix
#
# Para usar este archivo en lugar del generado:
#   cp /etc/nixos/hardware-configuration.nix hosts/nixos/hardware.nix
#   y descomentar la linea en default.nix

{ ... }: {
  imports = [ /etc/nixos/hardware-configuration.nix ];
}
